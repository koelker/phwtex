\begin{lstlisting}[language=XML,caption=Beschriftung,label=lst:beispiel-1]
<xml version="1.0" encoding="UTF-8"?>
<example>
  <text>Dies ist ein Beispiel für ein XML-Listing.</text>
</example>
\end{lstlisting}